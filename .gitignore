.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.env

# Runtime log files generated by the services (kept, but the .log
# contents shouldn't be committed; logs/.gitkeep keeps the folder itself
# tracked so `docker compose up` has somewhere to mount).
logs/*.log