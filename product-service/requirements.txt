Flask==3.0.3
prometheus-client==0.20.0

# opentelemetry-instrumentation still imports pkg_resources, which was
# removed from setuptools in newer releases; pin an older setuptools that
# still vendors it.
setuptools<81

opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-instrumentation-flask==0.48b0
